export const apiBasePath = `${config.SERVER_URL}/api/v2`;
export const stompUrl = `${config.SERVER_URL}/stomp`;

export const UPDATE_CHECK_INTERVAL = 60000; // check every min if there is a new version
