#
# Test configuration
#

#
# Datasource
spring.datasource.url=jdbc:h2:mem:db1;DB_CLOSE_DELAY=-1;MVCC=TRUE
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.show-sql=true

#
# Server sync
#
# use mocked server sync
mfprocurement.sync.url=
