# ArchUnit configuration for the cross-module architecture-tests aggregator.
# allowStoreCreation=true: the first run (no store) records the legacy baseline into ./archunit_store
# (committed to git). On later runs ArchUnit reports NEW cycles as failures and prunes fixed ones.
freeze.store.default.allowStoreCreation=true
