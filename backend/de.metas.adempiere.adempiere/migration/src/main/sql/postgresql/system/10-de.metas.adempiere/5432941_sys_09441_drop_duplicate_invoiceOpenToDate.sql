DROP FUNCTION IF EXISTS invoiceopentodate(numeric, numeric, date);

-- NOTE: we are keeping FUNCTION invoiceopentodate(p_c_invoice_id numeric, p_c_invoicepayschedule_id numeric, p_dateacct timestamp with time zone) only

