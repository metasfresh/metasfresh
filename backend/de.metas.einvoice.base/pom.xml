<project xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd"
	xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
	<modelVersion>4.0.0</modelVersion>

	<parent>
		<artifactId>de.metas.parent</artifactId>
		<groupId>de.metas</groupId>
		<version>10.0.0</version>
		<relativePath>../pom.xml</relativePath>
	</parent>

	<groupId>de.metas.einvoice</groupId>
	<artifactId>de.metas.einvoice.base</artifactId>
	<version>10.0.0</version>
	<packaging>jar</packaging>

	<dependencies>

		<dependency>
			<groupId>de.metas.business</groupId>
			<artifactId>de.metas.business</artifactId>
			<version>${project.version}</version>
		</dependency>

		<dependency>
			<groupId>de.metas.util</groupId>
			<artifactId>de.metas.util</artifactId>
			<version>${project.version}</version>
		</dependency>

		<dependency>
			<groupId>org.springframework</groupId>
			<artifactId>spring-context</artifactId>
		</dependency>

		<dependency>
			<groupId>org.projectlombok</groupId>
			<artifactId>lombok</artifactId>
			<scope>provided</scope>
		</dependency>

		<dependency>
			<groupId>de.metas.adempiere.adempiere</groupId>
			<artifactId>de.metas.adempiere.adempiere.base</artifactId>
			<version>${project.version}</version>
			<classifier>tests</classifier>
			<scope>test</scope>
		</dependency>

		<dependency>
			<groupId>org.assertj</groupId>
			<artifactId>assertj-core</artifactId>
			<scope>test</scope>
		</dependency>

		<dependency>
			<groupId>org.xmlunit</groupId>
			<artifactId>xmlunit-core</artifactId>
			<scope>test</scope>
		</dependency>

		<dependency>
			<groupId>org.xmlunit</groupId>
			<artifactId>xmlunit-assertj</artifactId>
			<scope>test</scope>
		</dependency>

	</dependencies>

	<profiles>
		<profile>
			<!--
			    Regenerate JAXB sources from XSD schemas in src/main/resources/**/*.xsd.
			    XSD source: Factur-X 1.08 EN16931 CII schema set, bundled from the
			    akretion/factur-x package (https://github.com/akretion/factur-x), BSD licence.
			    The XSDs are from the public Factur-X / ZUGFeRD 2.x EN16931 standard
			    (UN/CEFACT CII syntax, identical to XRechnung's CII variant).
			    Run this profile whenever a schema file changes:
			      mvn generate-sources -Pregenerate-jaxb-sources
			    Or regenerate all JAXB modules at once:
			      ./misc/dev-support/scripts/regenerate-jaxb-sources.sh
			    After regeneration, commit the changed java-xjc files together with the XSD change.
			-->
			<id>regenerate-jaxb-sources</id>
			<build>
				<plugins>
					<plugin>
						<groupId>org.jvnet.jaxb2.maven2</groupId>
						<artifactId>maven-jaxb2-plugin</artifactId>
						<executions>
							<execution>
								<id>generate-einvoice-cii-model</id>
								<phase>generate-sources</phase>
								<goals>
									<goal>generate</goal>
								</goals>
								<configuration>
									<args>
										<arg>-no-header</arg>
										<!-- needed to avoid java class name collisions across multiple XSDs -->
										<arg>-XautoNameResolution</arg>
										<!-- do not create package-info.java; we supply our own if needed -->
										<arg>-npa</arg>
									</args>
									<schemaDirectory>${project.basedir}/src/main/resources/de/metas/einvoice/cii/model</schemaDirectory>
									<generatePackage>de.metas.einvoice.cii.model</generatePackage>
									<generateDirectory>${project.basedir}/src/main/java-xjc</generateDirectory>
								</configuration>
							</execution>
						</executions>
					</plugin>
				</plugins>
			</build>
		</profile>
	</profiles>
</project>
