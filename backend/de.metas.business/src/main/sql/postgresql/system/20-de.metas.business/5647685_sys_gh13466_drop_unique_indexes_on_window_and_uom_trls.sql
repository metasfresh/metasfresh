-- Drop these temporarly until we fix the build and languages, duplicates are due to fr_CH and fr_FR elements
DROP INDEX IF EXISTS  idx_ad_window_trl_name_ad_language;
DROP INDEX  IF EXISTS idx_c_uom_trl_name_ad_language;