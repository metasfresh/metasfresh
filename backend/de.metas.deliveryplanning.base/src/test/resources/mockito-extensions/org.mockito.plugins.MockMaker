# This file ships INSIDE the module's test-jar (pom.xml builds one), so any module that ever depends
# on de.metas.deliveryplanning.base's test-jar inherits the inline mock maker for ALL of its own
# tests. No module does today; check this before adding such a dependency.
mock-maker-inline
