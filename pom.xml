<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<groupId>de.metas</groupId>
	<artifactId>de.metas.parent.general</artifactId>
	<version>3-development-SNAPSHOT</version>

	<packaging>pom</packaging>

	<description>Default parent pom with common settings (e.g. java file encoding) to be used by all maven projects</description>

	<url>http://www.metasfresh.com/</url>

	<organization>
		<name>metas GmbH</name>
		<url>http://www.metasfresh.com/</url>
	</organization>

	<inceptionYear>2015</inceptionYear>

	<developers>
		<developer>
			<name>metasfresh contributors</name>
			<organizationUrl>http://www.metasfresh.com/</organizationUrl>
			<email>contributors@metasfresh.com</email>
		</developer>
	</developers>

	<properties>
			<!-- 
			Please use this property when declaring metasfresh dependencies.
			Example:
			<dependency>
				<groupId>de.metas.commons.cxf</groupId>
				<artifactId>metasfresh-commons-cxf</artifactId>
				<version>${metasfresh-dependency.version}</version>
			</dependency>
			
			Using of ${metasfresh-dependency.version} is deprecated.
			See https://github.com/metasfresh/metasfresh-documentation/blob/master/infrastructure/CI_infrastructure.md for explanations.
			-->
		<metasfresh-dependency.version>[1-master-SNAPSHOT],[${project.version}]</metasfresh-dependency.version>

		<!-- java 1.7. is currently our minimum, but some projects might already be implemented with java-8-->
		<minimum.java.version>1.7</minimum.java.version>
	</properties>

	<repositories>
		<!-- 
		    The repositories that we relate on are defined in this parent POM, 
			because whether an artifact is found should not depend on a specific user's settings.xml 
		-->
		<repository>
			<id>metasfresh-repo</id>
			<name>metasfresh repository on github</name>
			<url>http://dedep901:8081/nexus/content/repositories/public</url>
		</repository>
	</repositories>

	<distributionManagement>
		<!-- 
		Where to deploy build artifacts; if you can't reach this repo, you can still make your own builds
		TODO: think about moving with the repo to a public place with much more storage :-) 
		-->
		<snapshotRepository>
     		<id>${repo-id}</id>
		    <name>${repo-name}</name>
      		<url>${repo-url}</url>
		</snapshotRepository>
	</distributionManagement>

	<build>
		<plugins>
			<plugin>
				<groupId>org.codehaus.mojo</groupId>
				<artifactId>properties-maven-plugin</artifactId>
				<version>1.0.0</version>
				<executions>
					<execution>
						<goals>
							<goal>set-system-properties</goal>
						</goals>
						<configuration>
							<properties>
								<!-- Avoid the build error "Failed to read schema document 'xjc.xsd', when using jaxb2-maven-plugin version 1.6.
									because 'file' access is not allowed due to restriction set by the accessExternalSchema 
									property." Thanks to http://stackoverflow.com/questions/34340115/is-there-a-generic-way-to-set-d-arguments-from-within-a-pom-xml -->
								<property>
									<name>javax.xml.accessExternalSchema</name>
									<value>all</value>
								</property>
							</properties>
						</configuration>
					</execution>
				</executions>
			</plugin>
		</plugins>

		<pluginManagement>
			<plugins>
				<!-- we set the metasfresh-wide encoding for code and resource files to be UTF-8 by default -->
				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-compiler-plugin</artifactId>
					<version>3.5.1</version>
					<configuration>
						<encoding>UTF-8</encoding>
						<source>${minimum.java.version}</source>
						<target>${minimum.java.version}</target>
					</configuration>
				</plugin>
				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-resources-plugin</artifactId>
					<version>3.0.0</version>
					<configuration>
						<encoding>UTF-8</encoding>
					</configuration>
				</plugin>

				<plugin>
					<groupId>org.codehaus.mojo</groupId>
					<artifactId>jaxb2-maven-plugin</artifactId>
					<!-- 
						later versions of the plugin don't work with eclipse's m2e plugin. 
						see https://github.com/mojohaus/jaxb2-maven-plugin/issues/24 for details 
						and updates on the issue 
					-->
					<!-- version>2.2</version -->
					<version>1.6</version>
				</plugin>

				<!--This plugin's configuration is used to store Eclipse m2e settings 
					only. It has no influence on the Maven build itself. -->
				<plugin>
					<groupId>org.eclipse.m2e</groupId>
					<artifactId>lifecycle-mapping</artifactId>
					<version>1.0.0</version>
					<configuration>
						<lifecycleMappingMetadata>
							<pluginExecutions>
								<pluginExecution>
									<pluginExecutionFilter>
										<groupId>com.vaadin</groupId>
										<artifactId>
											vaadin-maven-plugin
										</artifactId>
										<versionRange>
											[7.6.1,)
										</versionRange>
										<goals>
											<goal>compile-theme</goal>
											<goal>resources</goal>
										</goals>
									</pluginExecutionFilter>
									<action>
										<ignore></ignore>
									</action>
								</pluginExecution>
								<pluginExecution>
									<pluginExecutionFilter>
										<groupId>org.codehaus.mojo</groupId>
										<artifactId>properties-maven-plugin</artifactId>
										<versionRange>[1.0.0,)</versionRange>
										<goals>
											<goal>set-system-properties</goal>
										</goals>
									</pluginExecutionFilter>
									<action>
										<ignore></ignore>
									</action>
								</pluginExecution>
							</pluginExecutions>
						</lifecycleMappingMetadata>
					</configuration>
				</plugin>
			</plugins>
		</pluginManagement>
	</build>

	<profiles>
	  <!-- thx to http://stackoverflow.com/questions/635154/multiple-deployments-in-maven -->
	  <profile>
	    <id>metas-perm-snapshots-repo</id>
	    <properties>
		  <repo-id>metasfresh-perm-snapshots-repo</repo-id>
	      <repo-name>Maven metasfresh *permantent* Snapshots Repository</repo-name>
	      <repo-url>http://dedep901:8081/nexus/content/repositories/mvn-metasfresh-perm-snapshots/</repo-url>
	    </properties>
	  </profile>
	  <profile>
	    <id>metasfresh-snapshots-repo</id>
	    <activation>
	      <activeByDefault>true</activeByDefault>
	    </activation>
	    <properties>
	      <repo-id>metas-snapshots-repo</repo-id>
	      <repo-name>Maven metasfresh Snapshots Repository</repo-name>
	      <repo-url>http://dedep901:8081/nexus/content/repositories/metasfresh-snapshots/</repo-url>
	    </properties>
	  </profile>
	</profiles>

</project>