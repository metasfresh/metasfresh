
edit postgresql.conf:

#search_path = '"$user",public'         # schema names
# gh #489: adding the "dlm" schema to the search path, so by default our views like dlm.C_Invoice will be found.
search_path = '"$user",dlm,public'

# gh #489
custom_variable_classes = 'metasfresh'
metasfresh.DLM_ArchiveLevel = '1'
